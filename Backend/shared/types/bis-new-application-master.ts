import type { BisProjectMasterRow } from "./bis-project-master";

/** Row from `bis_new_applications` — same shape as `bis_projects` for shared UI helpers. */
export type BisNewApplicationMasterRow = BisProjectMasterRow;
